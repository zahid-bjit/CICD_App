pipeline {
    agent any
    stages {
        stage('Stop IIS') {
            steps {
                powershell 'invoke-command -computername app-srv -scriptblock {iisreset /STOP}'
            }
        }
    }
}
